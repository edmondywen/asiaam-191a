let box = 'myFirst box'

let number1 = 19

let message = `First variable: ${box} This is the second one ${number1}` //template literal format: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals

let messageBleh = "First variable: " + box + " This is the second one " + number1

let myFirstArray = [0, 1, 24, 44, "Hello!", messageBleh]

let myFirstObject = {"name":"Albert", "Location": "Los Angeles"} //javascript object notation JSON

// let - variable that can change, const - variable that can't change, var - acts as both. doesn't have a clear definition